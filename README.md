
  # High-fidelity e-commerce design

  This is a code bundle for High-fidelity e-commerce design. The original project is available at https://www.figma.com/design/gh4c7G3i44tiLzge6zVViM/High-fidelity-e-commerce-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  